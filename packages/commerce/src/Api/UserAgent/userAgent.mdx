# User Agent

Identifies the current device type (Android/Desktop/iOS). Returns a computed preact signal.

## Usage

```js
import userAgentSignal from '~/Api/UserAgent';

const userAgent = userAgentSignal.value;
```
